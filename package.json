{
  "name": "claude-chat-cleaner",
  "version": "0.1.0",
  "description": "Keyboard-driven chat cleaner for Claude conversations",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "postinstall": "wxt prepare",
    "test": "pnpm build && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "typescript": "^6.0.3",
    "wxt": "^0.20.26"
  }
}
